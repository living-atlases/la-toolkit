# Development overrides for docker-compose.yml
# Usage: cp .env.dev.example .env
#
# Then start dev services:
#   docker compose --profile dev up -d

# ChromaDB: expose on all interfaces and use local KB data
CHROMA_HOST=0.0.0.0
CHROMA_KB_PATH=./la_toolkit_backend/kb-data
ALLOW_RESET=true
